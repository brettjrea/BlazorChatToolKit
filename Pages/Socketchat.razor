@page "/socketchat"
@using Microsoft.AspNetCore.Components
@using Microsoft.AspNetCore.Components.Web
@using SocketIOClient
@inject NavigationManager NavigationManager

<h2>Socket Chat</h2>
<div>
    <input @bind="InputText" @onkeydown="InputKeyDown" />
    <button @onclick="SendMessage">Send</button>
</div>
<div>
    @foreach (var message in Messages)
    {
        <p>@message</p>
    }
</div>

@code {
    private string InputText { get; set; }
    private List<string> Messages { get; set; } = new List<string>();
    private SocketIO Socket { get; set; }

    protected override async Task OnInitializedAsync()
    {
        Socket = new SocketIO("http://localhost:5000");
        Socket.On("ChatResult", response =>
        {
            var message = response.GetValue<string>("output");
            InvokeAsync(() =>
            {
                Messages.Add(message);
                StateHasChanged();
            });
        });
        await Socket.ConnectAsync();
        
        // Configure the chat binary
        await Socket.EmitAsync("Configure", new { args = new string[] { }, chat_binary = "./chat" });
    }

    private async Task SendMessage()
    {
        if (!string.IsNullOrWhiteSpace(InputText))
        {
            await Socket.EmitAsync("Chat", new { input = InputText });
            InputText = string.Empty;
        }
    }

    private async Task InputKeyDown(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            await SendMessage();
        }
    }

    public async ValueTask DisposeAsync()
    {
        await Socket.DisconnectAsync();
    }
}
